import { Link } from "react-router-dom";import { Button, Form, Spinner } from "react-bootstrap";function ReviewForm({  isAuthenticated,  from,  myReview,  isEditing,  rating,  body,  isSubmitting,  onRatingChange,  onBodyChange,  onSubmit,  onEdit,  onDelete,  onCancel,}) {  if (!isAuthenticated) {    return (      <p className="text-muted mb-0">        <Link to="/login" state={{ from }}>          Login        </Link>{" "}        to add your rating and review.      </p>    );  }  return (    <>      {myReview && !isEditing && (        <div className="mb-3">          <p className="text-muted mb-2">            You've already reviewed or rated this film.          </p>          <div className="d-flex gap-3">            <Button size="sm" variant="outline-light" onClick={onEdit}>              Edit            </Button>            <Button size="sm" variant="outline-danger" onClick={onDelete}>              Delete            </Button>          </div>        </div>      )}      {(!myReview || isEditing) && (        <Form onSubmit={onSubmit}>          <Form.Group className="mb-3">            <Form.Label>Rating (1–10)</Form.Label>            <Form.Control              type="number"              min={1}              max={10}              value={rating}              onChange={(e) => onRatingChange(e.target.value)}              required            />          </Form.Group>          <Form.Group className="mb-3">            <Form.Label>Review (optional)</Form.Label>            <Form.Control              as="textarea"              rows={4}              value={body}              onChange={(e) => onBodyChange(e.target.value)}              placeholder="What did you think?"            />          </Form.Group>          <div className="d-flex gap-3">            <Button variant="warning" type="submit" disabled={isSubmitting}>              {isSubmitting ? (                <>                  <Spinner as="span" animation="border" size="sm" /> Saving...                </>              ) : myReview ? (                "Save changes"              ) : (                "Post review"              )}            </Button>            {myReview && isEditing && (              <Button variant="outline-light" type="button" onClick={onCancel}>                Cancel              </Button>            )}          </div>        </Form>      )}    </>  );}export default ReviewForm;